description: Nuevos atributos del Vehiculo
required: true
content:
  application/json:
    schema:
      type: object
      required:
        - id
      properties:
        modelo:
          $ref: ../schemas/Modelo_Vehiculo.yaml
        fecha:
          $ref: ../schemas/Fecha_Vehiculo.yaml
        matricula:
          $ref: ../schemas/Matricula_Vehiculo.yaml
        Estado:
          $ref: ../schemas/Estado_Vehiculo.yaml
